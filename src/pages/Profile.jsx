import React,{useEffect,useState}from'react';import{supabase}from'../lib/supabase.js';import{requireAuth,getUser}from'../lib/auth.js';import*as S from'../styles.js';
export default function Profile(){
  const[profile,setProfile]=useState(null);const[reviews,setReviews]=useState([]);
  useEffect(()=>{requireAuth(window.location.href);const u=getUser();if(!u)return;supabase.from('trust_profiles').select('*').eq('user_id',u.sub).single().then(({data})=>setProfile(data));supabase.from('trust_reviews').select('*').eq('reviewer_id',u.sub).order('created_at',{ascending:false}).then(({data})=>setReviews(data||[]));  },[]);
  if(!profile)return React.createElement('div',{style:S.page},React.createElement('p',{style:S.muted},'Loading...'));
  return React.createElement('div',{style:S.page},React.createElement('h1',{style:S.h1},'My Trust Profile'),React.createElement('div',{style:S.card},[['Trust Score',profile.score],['Level',profile.level],['Verified',profile.verified?'Yes':'No']].map(([k,v])=>React.createElement('div',{key:k,style:{display:'flex',justifyContent:'space-between',marginBottom:'0.5rem'}},React.createElement('span',{style:S.muted},k),React.createElement('span',{style:{color:'#e2e8f0',fontWeight:600}},v)))),React.createElement('h2',{style:{...S.h2,marginTop:'1.5rem'}},'Reviews Given'),reviews.length===0&&React.createElement('p',{style:S.muted},'No reviews given yet.'),reviews.map(r=>React.createElement('div',{key:r.id,style:S.card},React.createElement('p',null,'⭐'.repeat(r.rating)+' — '+r.service),React.createElement('p',{style:S.muted},r.comment))));
}