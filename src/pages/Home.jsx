import React,{useEffect,useState}from'react';import{supabase}from'../lib/supabase.js';import{requireAuth,getUser}from'../lib/auth.js';import*as S from'../styles.js';
export default function Home(){const[p,setP]=useState(null);useEffect(()=>{requireAuth(window.location.href);const u=getUser();if(!u)return;supabase.from('trust_profiles').select('*').eq('user_id',u.sub).single().then(async({data})=>{if(data){setP(data);}else{const{data:np}=await supabase.from('trust_profiles').insert({user_id:u.sub,score:50,level:'new',verified:false}).select().single();setP(np);}});},[]);
const c=s=>s>=80?'#22c55e':s>=50?'#f59e0b':'#dc2626';
return React.createElement('div',{style:S.page},React.createElement('h1',{style:S.h1},'IT-S Trust'),p&&React.createElement('div',{style:{...S.card,textAlign:'center',padding:'2rem'}},React.createElement('div',{style:{fontSize:'3rem',fontWeight:700,color:c(p.score)}},p.score),React.createElement('p',{style:{color:'#e2e8f0',fontWeight:600,marginTop:'0.5rem'}},p.level.toUpperCase()),React.createElement('p',{style:S.muted},p.verified?'✓ Verified':'Not yet verified')));}